export enum FeaturePageType {
  Index = 'index',
  NotFound = 'notFound',
  UrlBoard = 'url-board',
  GroupBoard = 'group-board',
  Account = 'account',
  Password = 'password',
}

export enum FeatureRouteType {
  All = '*',
  Match = '',
}
